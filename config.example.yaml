# Example configuration for AI CLI
# This shows different configuration options you can use

# Example 1: Default Docker Compose setup
ollama:
  url: "http://localhost:11434"
  model: "tinyllama"
  timeout: 120

# Local tinyollama configuration (Docker container fallback)
# This is used when remote ollama is not reachable or as a lightweight alternative
tinyollama:
  url: "http://localhost:11434"
  model: "tinyllama"
  timeout: 60
  # Heavy features disabled for tinyollama (lightweight model limitations)
  disable_code_mode: true
  disabled_features:
    - code_mode        # /code command - requires larger models for code generation
    - coder_model      # Code editing - requires specialized coder models
    - repomap_create   # Repository mapping - requires large context understanding
    - repomap_update   # Repository map update - requires large context understanding
    - datamap_create   # Data mapping - requires large context understanding
    - datamap_update   # Data map update - requires large context understanding

chat:
  system_prompt: "You are a helpful AI assistant."
  max_context_length: 10
  temperature: 0.7
  stream: true

---

# Example 2: Remote Ollama server with tinyollama fallback
# ollama:
#   url: "http://192.168.1.100:11434"  # Remote server IP
#   model: "mistral"
#   timeout: 180
#
# tinyollama:
#   url: "http://localhost:11434"      # Local Docker container
#   model: "tinyllama"
#   timeout: 60
#   disable_code_mode: true
#   disabled_features:
#     - code_mode
#     - coder_model
#     - repomap_create
#     - repomap_update
#     - datamap_create
#     - datamap_update

---

# Example 3: Code-focused assistant
# ollama:
#   url: "http://localhost:11434"
#   model: "codellama"
#   timeout: 120
# 
# chat:
#   system_prompt: "You are an expert programming assistant. Provide clear, concise code examples and explanations."
#   max_context_length: 15
#   temperature: 0.5
#   stream: true

---

# Example 4: Creative writing assistant
# ollama:
#   url: "http://localhost:11434"
#   model: "llama2"
#   timeout: 120
# 
# chat:
#   system_prompt: "You are a creative writing assistant. Help with storytelling, character development, and plot ideas."
#   max_context_length: 20
#   temperature: 0.9
#   stream: true

---

# Available Ollama models (examples):
# - tinyllama: Lightweight model (~1GB, CPU-friendly) - Default in Docker setup
# - llama2: General-purpose model
# - mistral: Fast and efficient model
# - codellama: Specialized for code
# - phi: Smaller, faster model
# - neural-chat: Conversational model
# - starling-lm: Instruction-following model
# 
# Docker Compose: docker compose exec ollama ollama pull <model-name>
# Local installation: ollama pull <model-name>
# CLI command: use 'models' to list available models
